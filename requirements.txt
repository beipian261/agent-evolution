# 智能进化系统依赖
# 使用方式: pip install -r requirements.txt
# 或者安装特定可选依赖: pip install chromadb prophet pandas

# 必需依赖
pandas>=1.3.0
numpy>=1.20.0

# 可选依赖 - 向量聚类 (ChromaDB)
# 用于语义聚类和向量相似度搜索
# 安装命令: pip install chromadb
chromadb>=0.4.0

# 可选依赖 - 时间序列预测 (Prophet)
# 用于错误率趋势预测和告警
# 安装命令: pip install prophet
# 注意: Prophet 在某些系统上需要先安装 cmdstanpy
prophet>=1.1.0

# 开发依赖 - 测试
pytest>=7.0.0
pytest-cov>=4.0.0

# 开发依赖 - 代码质量检查
flake8>=6.0.0
pylint>=3.0.0
black>=23.0.0
isort>=5.12.0

# 开发依赖 - 构建
build>=1.0.0
wheel>=0.40.0
setuptools>=65.0.0
